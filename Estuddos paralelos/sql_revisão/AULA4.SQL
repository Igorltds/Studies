CREATE DATABASE Teste
USE Teste

DROP table Cliente
CREATE TABLE Cliente
    ( NumCliente INT IDENTITY (1,1) PRIMARY KEY
    , CPF INT NOT NULL UNIQUE
    , RG INT NOT NULL UNIQUE
    , SEXO VARCHAR(10) CHECK(SEXO IN ('M', 'F', 'OUTROS'))
    , BONUS INT NOT NULL CHECK(BONUS > 100)
    , SALARIO DECIMAL (10, 4) DEFAULT 0)

INSERT INTO Cliente(CPF, RG, SEXO, BONUS, SALARIO) VALUES(62, 52, 'OUTROS', 500, NULL)

SELECT * FROM Cliente